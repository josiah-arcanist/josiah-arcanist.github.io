# Repo Agent Instructions

- This repo keeps exactly one commit. Squash or amend all work into the existing commit so `git log` shows a single commit, then force-push.
- Commit with the GitHub noreply author email (`42162445+josiah-arcanist@users.noreply.github.com`). Pushes that expose a private email are rejected.
